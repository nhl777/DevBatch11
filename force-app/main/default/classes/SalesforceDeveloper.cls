public  class SalesforceDeveloper {
    public void myFavorites(){
        string city = 'Bingöl';
        integer favNumber = 11;

        system.debug(city);
        system.debug(favNumber);
        system.debug('Favori Numaram -> ' + favNumber + ' ' + 've ' + 'Bulunduğum Şehir -> ' + city);
    }
}
